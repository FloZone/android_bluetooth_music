<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="16dp"
    android:focusable="true"
    android:focusableInTouchMode="true">

    <ListView
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:paddingLeft="16dp"
        android:paddingRight="16dp">

        <ImageButton
            android:id="@+id/btnOtherPlayer"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"
            android:src="@drawable/ic_done_black_36dp"
            android:contentDescription="@string/done"
            android:background="@color/colorBackground" />

        <!--TODO vérifier le inputType le plus adequat-->
        <EditText
            android:id="@+id/etOtherPlayer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_alignParentStart="true"
            android:layout_toStartOf="@id/btnOtherPlayer"
            android:layout_marginEnd="16dp"
            android:textSize="14sp"
            android:inputType="textUri"
            android:lines="1"
            android:hint="@string/other_player_package" />

    </RelativeLayout>

</LinearLayout>